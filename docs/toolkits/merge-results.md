# 5.4 Merge result helpers

```bash
merge_length -h
merge_saturation -h
merge_digestion -h
merge_offset_detail -h
merge_offset -h
merge_dst_list -h
merge_period -h
merge_metagene -h
merge_coverage -h
merge_quant -h
merge_pausing -h
merge_occupancy -h
merge_cdt -h
merge_cst -h
merge_odd_ratio -h
```

Most commands follow:

```text
usage: merge_xxx [-h] -l LIST [LIST ...] -o OUTPUT
```
